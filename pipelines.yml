resources:
  - name: autoTrigger
    type: CronTrigger
    configuration:
      interval: '30 11 * * *'
  - name: helloWorldContainerImage
    type: Image
    configuration:
      registry: JFrogCR
      sourceRepository: default-docker-virtual
      imageName: hello-world
      imageTag: latest
      
pipelines:
  - name: CronPipeline
    steps:
      - name: ImageList
        type: Bash
        configuration:
          integrations: [
            helloWorldContainerImage
          ]
          inputResources:
            - name: autoTrigger
        execution:
          onExecute:
            - docker image ls
    
