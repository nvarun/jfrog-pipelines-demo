resources:
  - name: nginxContainerImage
    type: Image
    configuration:
      registry: JFrogCR
      sourceRepository: default-docker-virtual
      imageName: library/nginx
      imageTag: latest
